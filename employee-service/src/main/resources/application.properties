spring.application.name=Ems-backend
server.port=9099
# MySQL 数据库配置
spring.datasource.url=jdbc:mysql://rm-bp127693aheq21j8lno.mysql.rds.aliyuncs.com:3306/davinci?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 配置 Hibernate JPA
#spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
# Hibernate 会根据实体类自动更新数据库表结构，如果表已经存在，则不会删除。
spring.jpa.hibernate.ddl-auto=update


##################无效####################
# 每次启动时都会删除旧表并创建新表。
#spring.jpa.hibernate.ddl-auto=create
# 禁用 Hibernate 自动创建表结构
#spring.jpa.hibernate.ddl-auto=none
# 配置 Spring Boot 加载 schema.sql
#spring.datasource.schema=classpath:schema.sql

